export interface foodInterface {
  foodId?: number;        // optional because backend generates it
  foodName: string;
  foodCategory: string;
  foodPrice: number;
  foodimg: string;
  foodRating: number;
}
