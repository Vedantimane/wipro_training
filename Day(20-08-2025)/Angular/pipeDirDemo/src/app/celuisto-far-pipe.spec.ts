import { CeluistoFarPipe } from './celuisto-far-pipe';

describe('CeluistoFarPipe', () => {
  it('create an instance', () => {
    const pipe = new CeluistoFarPipe();
    expect(pipe).toBeTruthy();
  });
});
